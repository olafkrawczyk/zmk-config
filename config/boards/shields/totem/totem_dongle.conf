# Prospector dongle options (applies to the totem_dongle build only)

# Active screen: bongo cat (taps along with typing, shows WPM + layer name)
CONFIG_PROSPECTOR_STATUS_SCREEN_BONGO=y
# Info-dense screen with numeric WPM meter:
# CONFIG_PROSPECTOR_STATUS_SCREEN_OPERATOR=y

# No ambient light sensor populated: use fixed brightness
CONFIG_PROSPECTOR_USE_AMBIENT_LIGHT_SENSOR=n
CONFIG_PROSPECTOR_FIXED_BRIGHTNESS=80
